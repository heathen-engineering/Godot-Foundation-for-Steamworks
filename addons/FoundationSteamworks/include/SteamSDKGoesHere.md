The Steamworks SDK is owned by Foundation for Steamworks.

Copy the Steamworks SDK folder to the Foundation include folder instead.

The result should be that
`<YourProjectRoot>/addons/FoundationSteamworks/include/sdk/public/steam/steam_api.h` is a valid file path.
